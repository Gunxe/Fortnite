<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FürFortnite" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:fontFamily">@font/burbank_bold</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple</item> <!-- Navbar Farbe -->
        <item name="colorPrimaryVariant">@color/purple_500</item> <!-- oben bei der Zeit -->
        <item name="colorOnPrimary">@color/white</item> <!-- keine Ahnung -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item> <!-- keine Ahnung -->
        <item name="colorSecondaryVariant">@color/teal_700</item> <!-- keine Ahnung -->
        <item name="colorOnSecondary">@color/black</item> <!-- keine Ahnung -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.FürFortnite.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FürFortnite.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FürFortnite.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>